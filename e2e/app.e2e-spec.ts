/*
 import {MicrofinancePage} from './app.po'

 describe('microfinance App', () => {
 let page: MicrofinancePage;

 beforeEach(() => {
 page = new MicrofinancePage();
 });

 it('should display message saying app works', () => {
 page.navigateTo();
 expect(page.getParagraphText()).toEqual('app works!');
 });
 });
 */
